# Documents for ${{values.app_name}}

This applications has 2 endpoints:
- http://python-apps.test.com/api/v1/info
- http://python-apps.test.com/api/v1/healthz

Here we can expand what each of the endpoints do

# How to access the app?
You can access the app by accessing this url `${{values.app_name}}-${{values.app_env}}.test.com/api/v1/details`